import styled from 'styled-components';
import { Card } from '@material-ui/core';

const StyledComicCard = styled(Card)`
  background-color: #171717;
  color: #ffffff;
`;

export default StyledComicCard;
